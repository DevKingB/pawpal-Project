[pytest]
# Default flags: verbose + short traceback + force colors
addopts = -v --tb=short --color=yes

# Test discovery
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
